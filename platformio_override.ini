[platformio]
default_envs = hichi32c3

[env:hichi32c3]
extends = env:tasmota32_base
board = esp32c3

build_flags =
    ${env:tasmota32_base.build_flags}
    -DUSE_CONFIG_OVERRIDE
    -DCODE_IMAGE_STR='"hichi-sml"'
    -DOTA_URL='""'

lib_ignore =
    ${env:tasmota32_base.lib_ignore}
    Micro-RTSP
